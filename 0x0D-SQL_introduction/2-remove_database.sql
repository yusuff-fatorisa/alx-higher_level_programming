-- This script contains some 'SQL' commands that
-- deletes the specified database. In case the name of
-- the specified database does not exist, the code does not fail


DROP DATABASE IF EXISTS hbtn_0c_0;
