-- This script contains some SQL commands that computes
-- the score average of all records in the table 'second_table'
-- of the 'hbtn_0c_0' database of your MySQL server.

SELECT AVG(score) average FROM second_table
